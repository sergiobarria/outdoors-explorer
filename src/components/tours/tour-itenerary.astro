---
import type { TourDetailWithDescription } from '@/db/types';

interface Props {
	tour: TourDetailWithDescription;
}

// const { tour } = Astro.props as Props;
---

<section class="rounded-lg bg-white p-6 shadow-md">
	<h3 class="mb-4 text-xl font-semibold">Tour Itinerary</h3>
	<p class="mb-4 text-gray-600">A day-by-day breakdown of your adventure:</p>
	{
		[1, 2, 3, 4, 5, 6, 7].map((day) => (
			<div class="mb-4">
				<h4 class="mb-2 text-lg font-semibold">Day {day}</h4>
				<p class="text-gray-600">
					Lorem ipsum dolor sit amet, consectetur adipiscing elit. Vivamus lacinia odio
					vitae vestibulum.
				</p>
			</div>
		))
	}
</section>
